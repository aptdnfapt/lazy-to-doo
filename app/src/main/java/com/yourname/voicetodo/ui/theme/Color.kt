package com.yourname.voicetodo.ui.theme

import androidx.compose.ui.graphics.Color

// Primary accent blue - vibrant but professional
val Primary = Color(0xFF3B82F6)      // Modern blue
val PrimaryVariant = Color(0xFF2563EB)  // Darker blue variant

// Black-based theme colors
val Black = Color(0xFF000000)           // Pure black
val NearBlack = Color(0xFF0A0A0A)       // Very dark black
val DarkGray = Color(0xFF1A1A1A)        // Dark gray for depth
val MediumGray = Color(0xFF2A2A2A)      // Medium gray for surfaces
val LightGray = Color(0xFF404040)       // Light gray for borders
val MutedGray = Color(0xFF6B7280)       // Muted gray for secondary text

// Surface colors with subtle variations
val Surface = Color(0xFF0F0F0F)          // Main surface
val SurfaceVariant = Color(0xFF171717)   // Alternative surface
val SurfaceElevated = Color(0xFF1F1F1F)  // Elevated surfaces

// Text colors
val OnSurface = Color(0xFFF5F5F5)        // High contrast white
val OnSurfaceSecondary = Color(0xFF9CA3AF) // Secondary text

// Light theme colors (maintained for completeness)
val BackgroundLight = Color(0xFFF8FAFC)
val Gray200 = Color(0xFFE5E7EB)
val Gray800 = Color(0xFF1F2937)
val White = Color(0xFFFFFFFF)

// Semantic colors
val Success = Color(0xFF10B981)          // Green for success
val Warning = Color(0xFFF59E0B)          // Amber for warning
val Error = Color(0xFFEF4444)            // Red for errors